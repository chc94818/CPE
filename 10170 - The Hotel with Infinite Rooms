/*
等比級數問題
*/
#include <iostream>
#include <stdlib.h>
#include <stdio.h>
using namespace std;

int main(){
	  long long int s, d;
    while(scanf("%lld %lld", &s, &d) == 2) {
		long long int sum = 0;
		
		while(sum<d){
			sum+=s;
			s++;
		}
		cout <<s-1<<endl;
	}
	return 0;
}
